## jQuery `scrollParent` plugin

This plugin is a standalone solution for those wishing to use jQuery UI's `scrollParent` method without using jQuery UI itself.

Install via bower:

```sh
$ bower install jquery-scrollparent
```
